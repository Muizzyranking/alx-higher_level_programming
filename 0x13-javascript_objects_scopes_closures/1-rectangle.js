#!/usr/bin/node
// creates a class Rectangle that defines a rectangle
// with width and height

class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
